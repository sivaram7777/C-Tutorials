#include<stdio.h>
#include<math.h>
int main() {
    int a;

    //To Check whether the character is digit or not

    printf("Enter the Character : \n");
    scanf("%d", &a);

//If the Character is a digit then return True (1), 
//If the Character is not a digit then return False (0)

    printf("The entered character is: %d \n",a > 0 && a < 10 ==1);

}