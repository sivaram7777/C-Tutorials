#include <stdio.h>
int main() {
    float a;
    printf("enter the side of the square \n");
    scanf ("%f", &a);

    float area = (a * a);
    printf("The area of square is : %f \n", area);
    return 0;
}